# Entregas

Cada estudante cria aqui uma pasta com o próprio nome, no formato
`sobrenome-nome`, contendo:

```
sobrenome-nome/
  ingressos.html
  css/estilo.css
  RELATORIO.md
```

As imagens não precisam ser reenviadas. Ajuste o caminho no seu HTML para
`../../img/`.
